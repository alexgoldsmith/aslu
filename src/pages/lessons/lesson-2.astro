---
import { SITE } from "~/config.mjs";
import LessonLayout from "~/layouts/LessonLayout.astro";

const title = `${SITE.org} | Lesson-2`;
const description = `Lesson 2 from ${SITE.org}`;
const canonical = new URL(`lesson-2`, Astro.site);
---

<LessonLayout meta={{ title, description, canonical }}>
    <main></main>
</LessonLayout>
